# 뮤즐리(Muselry) 프로젝트 작업 명세서 v1.7

**작성일:** 2026-05-13
**작성자:** Manus AI
**버전:** v1.8

---

## v1.8 변경 요약

- **탐색 필터 구조 재정비 (3.1)**: 추천 태그, 유형 필터, 운영 주체 필터를 3분류로 분리하고 UI/DB 매핑 정책 수립.
- **데이터 정합성 보완 (v1.7)**: Day 3 시드 데이터 누락으로 인한 546건 이슈 확인. 국립/공립 525건에 대한 데이터 정합성 확보 방안 수립.
- **Phase 1.5 신설 (v1.7)**: 리뷰 좋아요 기능(`review_likes` 테이블), 푸시 알림 기초 설정(APNs/FCM)을 MVP 직후 Phase 1.5로 이관하여 MVP(Day 10) 안정성 확보.
- **Day 10 범위 확정 (v1.7)**: 기능 추가 없이 통합 QA, 버그 픽스, 테스트 빌드 추출에 집중하도록 Day 10 범위를 전면 재조정. 아바타 변경 기능은 선택 사항으로 분류.

---

## 1. 서비스 개요 및 핵심 가치

**"뮤즐리는 박물관에서 시작해, 미술관과 다양한 문화공간으로 확장하며 국민의 문화 향유를 높이는 라이프스타일 앱을 지향한다."**

**뮤즐리(Muselry)**는 초기에는 전국 박물관·미술관 탐색 및 방문 기록 앱으로 시작하지만, 장기적으로는 어린이 체험관, 과학관, 자연사관, 생태관, 사설 문화공간, 지역 문화 명소까지 포괄하는 문화공간 발견·기록·추천 플랫폼으로 확장합니다.

짧은 비전: "박물관에서 시작해, 문화공간 전체로 확장하는 국민 문화 향유 앱."

### 1.1 단계별 확장 로드맵

**Phase 1 — 박물관 코어, MVP**
- **목표**: 등록 박물관 중심 데이터 구축 및 기본 구조(탐색, 검색, 지도, 북마크, 방문 기록, 리뷰, 인기 탭) 완성
- **주의**: MVP에서는 박물관·미술관 중심으로 범위를 제한하며, 사설 문화공간의 무리한 추가는 금지합니다. [1]

**Phase 1.5 — 기능 고도화 및 안정화, MVP+1개월 (NEW)**
- **목표**: 사용자 인터랙션 강화 및 리텐션 도구 도입
- **확장 기능**: 리뷰 좋아요 기능, 푸시 알림 기초 설정(APNs/FCM 연동), 아바타 변경 기능(MVP 미포함 시)

**Phase 2 — 미술관 강화, MVP+3개월**
- **목표**: 박물관·미술관 양대 카테고리 강화
- **추가 검토**: 전시 정보 데이터 연동, 미술관 전용 필터, 전시 중심 큐레이션, 데이트/혼자 보기 좋은 전시 추천

**Phase 3 — 문화공간 확장, MVP+6개월**
- **목표**: 법적 등록 박물관·미술관을 넘어 사용자 관점의 문화공간으로 확장
- **확장 대상**: 사설 어린이 체험관, 우주센터, 공룡·자연사·곤충·생태 체험관, 과학 체험관, 독립영화관, 공연장, 도서관, 지역 문화공간
- **데이터 보강**: 운영팀 수동 큐레이션, 네이버 지도/지자체 콘텐츠 참고, 운영자 포털 직접 등록

**Phase 4 — 문화 생활 동반자, MVP+1년**
- **목표**: 사용자가 일상에서 문화를 즐기고 기록하는 앱
- **확장 기능**: 전시 종료 알림, 문화 다이어리, 취향 기반 추천, Muselry Trip, 테마별 문화 코스

### 1.2 MVP 핵심 기능

- 탐색 및 검색: 전국 박물관·미술관 정보 조회, 지역/유형 필터링, 키워드 검색
- 지도 기반 탐색: 네이버 지도 연동을 통한 내 주변 및 특정 지역 문화공간 탐색
- 방문 기록 관리: '가고 싶어요(북마크)', '다녀왔어요(방문 완료)', 별점, 개인 메모 작성
- 커뮤니티: 사용자 공개 리뷰 작성 및 피드 조회
- 사용자 관리: 마이페이지, 프로필 편집

---

## 2. 기술 스택 및 의사결정

| 분류 | 확정 기술/정책 | 상세 설명 |
|------|----------------|----------|
| 앱 프레임워크 | Flutter | 단일 코드베이스로 iOS/Android 동시 개발 |
| 상태 관리 | Riverpod | 앱 내 전역 상태의 효율적 관리 |
| 백엔드/BaaS | Supabase | PostgreSQL 기반 DB, Auth, Storage, RLS 활용 |
| 지도 서비스 | Naver Map API | 국내 사용자 친화적 지도 SDK |
| 인증(Auth) | Supabase Auth | 이메일/비밀번호, Kakao 로그인, Google 로그인, Apple 로그인 |
| 데이터 연동 | DB 사전 적재 방식 | CSV/JSON 가공 후 DB 초기 적재 |

---

## 3. 화면 및 UI/UX 구성

### 3.1 주요 화면 상세

- 홈 화면: 상단 통합 검색창, 내 주변 박물관, 테마별 추천, 인기 박물관 카드 섹션(가로 스크롤 리스트)
- 탐색 화면: 검색 및 필터 버튼(지역, 추천 태그, 유형, 운영), 박물관 썸네일 카드 리스트
- 지도 화면: 네이버 지도 SDK 연동, 현재 위치 처리, **마커 클러스터링**, 유형별 마커 표시, 마커 탭 시 상세 화면 이동, 길찾기 딥링크/웹 폴백
  - *MVP 이후 개선 후보*: 마커 탭 시 하단 요약 Bottom Sheet
- 상세 화면: 대표 이미지 Parallax 스크롤, 북마크/방문기록 토글, 길찾기 연동. 리뷰 작성 시 `editableDaysLeft` 배지(수정 가능 잔여 일수) 표시. 어린이 추천 배지 및 `kids_note` 섹션 표시.
- 기록 화면: 다녀온 곳 / 가고 싶은 곳 리스트, 개인 메모 모아보기
- 커뮤니티 화면: 최신/인기 리뷰 피드, 좋아요/신고 기능
- **마이페이지**: 프로필, 방문 통계, 북마크 목록, 내 활동 내역
  - **내가 다녀온 박물관 지도**:
    - **마커 스타일**: 방문(진한 색+채워짐, 횟수에 따른 크기/숫자 표시), 북마크(연한 색+별 표시), 미방문(작은 점)
    - **필터**: 전체/방문/북마크, 유형별, 기간별
    - **통계 오버레이**: "총 N곳 방문", "이번 달 N곳 방문", "가장 많이 방문한 지역"
    - **인터랙션**: 방문 마커(방문 기록 표시), 미방문 마커(정보+북마크 버튼)
    - **클러스터링**: 전국 데이터 표시를 위한 클러스터링 적용

**탐색 화면 필터 구조 (v1.8 재정비)**
탐색 화면의 필터는 사용자 인지 부하를 줄이고 확장성을 확보하기 위해 다음 3분류 구조로 운영합니다:
1. **추천 태그**: "아이와 함께" (MVP), 향후 데이트, 시니어, 외국인 등 확장
2. **유형 필터**: 전체, 박물관, 미술관, 과학관, 기념관, 전시관 (`museums.type` 기준)
3. **운영 필터**: 전체, 공공, 민간
   - 공공 = 국립 + 공립
   - 민간 = 사립 + 대학 + 기업

*운영 주체 매핑 엣지 케이스 정책*:
DB의 운영 주체 값(`museums.ownership`)은 국립/공립/사립/대학/기업 원본 값을 그대로 유지하며, UI 필터에서만 공공/민간으로 그룹핑하여 표시합니다. 대학 부설이지만 사실상 정부 출연인 경우 등 모호한 케이스는 원본 값 기준으로 기계적 분류(대학 → 민간)하며, 예외 처리가 필요한 경우 운영자가 Supabase Dashboard에서 `ownership` 값을 직접 수정하여 반영합니다.

---

## 4. 데이터베이스 설계 및 핵심 정책

### 4.1 데이터베이스 구조 (Supabase)

| 테이블 | 주요 필드 | 설명 |
|--------|-----------|------|
| profiles | id, nickname, avatar_url | Auth 연동 사용자 프로필 |
| museums | id, name, type, average_rating, review_count, is_kids_friendly, kids_note | 박물관 기본 정보 및 리뷰 통계 |
| bookmarks | id, user_id, museum_id | 가고 싶은 곳 저장 내역 |
| visits | id, user_id, museum_id, visited_at, rating, private_note | 방문 완료 및 개인 메모 기록 |
| reviews | id, user_id, museum_id, **visit_id**, rating, content, status | 커뮤니티 공개 리뷰. **1방문 1리뷰 부분 인덱스 강제**, visit_id NOT NULL FK |
| review_reports | id, review_id, reporter_id, reason | 리뷰 신고 관리 |
| museum_ranking | museum_id, bayesian_score, updated_at | 인기 점수 계산 캐싱 |

**장기 확장 메모**
현재 MVP에서는 `museums` 테이블을 사용하지만, Phase 3 이후 문화공간 전체를 포괄할 경우 `cultural_places` 또는 `venues` 개념으로 확장이 가능합니다. Phase 3 진입 시점에 테이블 리네이밍 또는 `place_category`, `legal_type`, `source_type`, `verification_status` 필드 확장을 검토합니다.

### 4.2 평점 컬럼 분리 정책

- **`museums.average_rating` 및 `review_count`**: 단순 산술 평균 (상세 화면·탐색 카드 별점 표시용)
  - **집계 대상**: `reviews.status = 'published'` 리뷰만 포함 (`pending_review`, `hidden`, `removed` 제외)
  - **갱신**: 리뷰 INSERT/UPDATE/DELETE 모두에서 트리거(`trg_update_museum_rating`) 발동. `status` 변경 시(UPDATE)에도 재계산 필수
- **`museum_ranking.bayesian_score`**: 베이지안 평균 (인기 탭 정렬용, BM 1 광고 슬롯 배치 기준)
  - 공식: `score = (리뷰수 × 평균별점 + C × 전체평균별점) / (리뷰수 + C)`, C 초기값 10
  - 갱신: 일 1회 배치 (백업 플랜: 수동 refresh RPC 허용)

### 4.3 어린이 친화 자동 분류 정책

자동 룰은 오탐을 방지하기 위해 보수적으로 운영하며, 누락 케이스는 운영자가 수동으로 보정합니다. 향후 데이터 추가 시에도 동일한 원칙을 적용합니다.

- **강한 단독 키워드**: 어린이, 아동, 키즈, 유아 (이름 또는 설명에 포함되면 단독으로 `true` 처리)
- **일반 조건부 키워드**: 체험, 가족, 교육, 놀이 (단독으로는 분류하지 않고, 2개 이상 매칭 시에만 `true` 처리)
- **보조 키워드**: 과학, 생태 (강한 또는 일반 키워드와 AND 조건 시에만 `true` 처리)
- 수동 보정: Supabase Dashboard에서 운영자가 직접 보정 가능
- `kids_note`: 운영자 수동 입력 자유 텍스트

### 4.4 리뷰 status 값 정의 및 1방문 1리뷰 정책

- **1방문 1리뷰**: 한 사용자는 같은 방문 기록(visit_id)에 대해 활성 리뷰를 1개만 작성 가능합니다. `reviews_unique_active_visit` 부분 인덱스로 강제되며, `status = 'removed'`인 리뷰는 예외입니다.
- `published`: 정상 노출
- `pending_review`: 자동 필터링 의심 판정, 운영자 검토 대기. UI에 "운영자 확인 후 공개됩니다" 안내 표시.
- `hidden`: 신고 누적 또는 운영자 판단으로 비공개
- `removed`: 작성자 본인 소프트 삭제. `average_rating` 계산에서 제외되며, 일반 사용자에게 노출되지 않음. 삭제 후 새 리뷰 작성 가능.

---

## 5. 수익 모델 및 장기 확장 전략 (BM)

### 5.1 BM 1 — 인기/추천 탭 (광고 슬롯 연계)

- 성격: 앱의 핵심 리텐션(Retention) 기능
- 인기 점수 공식: 베이지안 평균 적용
- 원칙: 광고/유료 추천 카드는 반드시 "AD", "스폰서" 배지로 명확히 표시

### 5.2 BM 2 — Muselry Trip (큐레이션 여행 상품화)

Muselry Trip은 Muselry가 단순 정보 앱에서 문화 향유 플랫폼으로 확장되는 단계에서 등장하는 장기 BM입니다. 정보 제공 → 큐레이션 → 실제 문화 경험의 정점으로 이어집니다. 사용자의 방문 기록, 리뷰, 큐레이션 데이터를 기반으로 지역 문화 코스를 제안하고, 충분한 MAU와 engagement가 확보된 뒤 소규모 파일럿으로 검증합니다. 예약/결제/여행업 운영은 MVP 범위에서 제외합니다.

### 5.3 BM 3 — 사설 박물관 유료 노출 (운영자 포털 단계화)

운영자 포털은 MVP 범위가 아닙니다 (RBAC 설계, 소유권 검증, 허위 운영자 방지, 분쟁 처리 등 복잡도 높음). 아래와 같이 3단계로 점진적 도입합니다.

**Phase 3 (MVP+6개월): 운영팀 수동 큐레이션**
- 어린이 친화 사설 시설 등 30~50곳을 운영팀이 직접 큐레이션
- 워크플로우: Notion/스프레드시트로 후보 정리 → 정보 검수 → DB 수동 추가

**Phase 4 초입 (MVP+9~12개월): 간소화된 운영자 포털 베타**
- 기능 범위: 운영자 로그인, 기관 소유권 신청(사업자등록증 제출), 본인 기관 정보 수정 요청(`pending_review` 후 관리자 승인 반영)
- 대상: 베타 참여 기관 10~20곳 (무료 운영)

**Phase 4 후기 (MVP+12개월~): 정식 운영자 포털 + 유료화**
- 기능: 운영자 대시보드(조회수, 북마크 수 등 통계), 이미지/운영시간 수정, 프리미엄 노출 구매, 리뷰 답변 기능
- BM: 프리미엄 기관 페이지, 추천/인기 탭 스폰서 슬롯, 지역/테마별 유료 노출

---

## 6. 커뮤니티 모더레이션 및 작성 UX 정책

- **6.1 자동 필터링**: `KoreanContentFilter` 클래스 적용 (Dart 한국어 욕설 필터 라이브러리). 욕설/스팸/URL/전화번호 감지 시 완전 차단하지 않고 `reviews.status = 'pending_review'`로 저장. 분기별 사전 업데이트 정책.
- **6.2 신고 처리 (`review_reports`)**:
  - `UNIQUE(review_id, reporter_id)` 제약으로 중복 신고 차단
  - `(review_id)` 인덱스로 누적 카운트 성능 보강
  - RLS 정책: 로그인 사용자만 신고 가능, 본인 리뷰 신고 금지
  - 누적 3건 이상 신고 시 `reviews.status = 'hidden'` 자동 전환
- **6.3 작성자 권한 및 1방문 1리뷰 UX**:
  - 작성 후 7일 이내 본인 수정 가능(`editableDaysLeft` 배지 UI 표시). 소프트 삭제(`status = 'removed'`)는 언제든 가능.
  - **방문 기록 필수**: 방문 기록이 없는 경우 리뷰 작성 불가 ("리뷰를 쓰려면 먼저 방문 기록을 남겨주세요" 안내).
  - **방문 선택**: 방문 기록이 여러 개인 경우 어떤 방문에 대한 리뷰인지 선택.
  - **기존 리뷰 존재 시**: "리뷰 작성 완료 / 수정하기" 버튼 표시.
- **6.4 운영 페이지**: 개인정보 처리방침, 서비스 이용약관, 신고/문의 접수 채널 (스토어 심사 필수)

---

## 7. 백로그 (Phase 1.5 이후 확장)

**기능 고도화 및 안정화 (Phase 1.5)**
- 리뷰 좋아요 기능 (`review_likes` 테이블 신설 및 UI 연동)
- 푸시 알림 기초 설정 (APNs/FCM 설정, Edge Function, 권한 동의 플로우)
- **데이터 정합성 정리**: 잔존 시드 21건 제거 후 국립/공립 525건 재적재. 현재 DB에는 국립/공립 525건 + 시드 잔존 21건 = 546건이 존재하며, MVP 출시 후 안정화 단계에서 `TRUNCATE` 후 정식 525건으로 재적재합니다.

**평점 가중치 및 신뢰도 고도화 (Phase 2)**
- 사용자별 최신 리뷰만 ranking에 반영하는 방식 검토
- 반복 리뷰 가중치 제한 검토
- 방문 인증 기반 리뷰 신뢰도 가중치 검토

**사설 어린이·가족 문화공간 수동 큐레이션 (Phase 3)**
- 공공데이터에 없는 사설 가족 문화공간 30~50곳을 운영팀이 수동 큐레이션으로 추가 검토합니다. (예: 옥토끼우주센터)

**테마 키워드 확장 (Phase 3)**
- 우주, 천문, 항공, 공룡, 곤충, 동물, 자연사, 로봇, 장난감, 해양, 생태, 과학 키워드를 어린이·가족 문화공간 후보로 적극 활용합니다. 단, 성인/야간 관측 특화 시설은 제외합니다.

**모더레이션 고도화 (Phase 2)**
- 2차 방어선으로 OpenAI Moderation API 도입 검토
- Dart 키워드 사전 분기별 업데이트 정책 수립

---

## 8. 12~14일 개발 스프린트 현황

| 일차 | 단계 | 진행 상태 | 주요 내용 |
|------|------|-----------|----------|
| Day 1~4 | 환경/인증/목록/상세 | 완료 | Supabase DB 구축, Auth 연동, 무한 스크롤 탐색, 상세 화면 구현 |
| Day 5 | 지도 연동 | 완료 | 네이버 지도 SDK 연동, 현재 위치, 마커 탭 상세 이동, 길찾기 |
| Day 6 | 방문 기록 | 완료 | Visit 모델/Provider 구현, 다녀왔어요 Bottom Sheet, 방문 기록 목록 |
| Day 7 | 마이페이지 | 완료 | Profile 모델 구현, 마이페이지 전면 재작성 (방문 통계, 북마크 섹션) |
| Day 8 | 리뷰 기능 및 마이페이지 지도 | 완료 | 1방문 1리뷰 정책 재설계, 마이페이지 지도 시각화, 마커 클러스터링 |
| Day 9 | 홈 & 커뮤니티 | 완료 | 홈 화면 "인기 박물관" 섹션 (가로 스크롤 카드 리스트), 커뮤니티 피드, 탐색 화면 어린이 친화 필터 칩, 상세 화면 어린이 배지/kids_note, museum_ranking 배치 로직 (수동 refresh RPC) |
| **Day 10** | **QA & 빌드** | **예정** | **[필수] 전체 플로우 통합 QA, 버그 픽스, Android/iOS 테스트 빌드 추출. [선택] 아바타 변경 기능** |

*Day 10 작업 범위 명확화*:
- 기능 추가를 멈추고 통합 QA에 집중합니다.
- 리뷰 작성/수정/삭제/신고, 1방문 1리뷰 정책, 마이페이지 지도, 인기 박물관 섹션, 어린이 필터/배지/kids_note, 로그인/로그아웃/북마크/방문 기록/지도/상세 화면을 통합 테스트합니다.
- flutter analyze 경고(Riverpod deprecated info 등)를 모두 정리합니다.

## References
[1] Data Curation Strategy for MVP Stages: Prioritize official and larger institutions over private or smaller ones to manage scope and ensure data quality.
