# 버그: <제목>

> 파일명 예: `naver-map-auth-failed-blank.md`. 재발 시 시간 단축이 목적. 터미널 로그 전문 저장 금지.

- **증상(symptom)**:
- **근본 원인(root cause)**:
- **증거(evidence)**(어떻게 확인했나 — 코드/로그 요지/재현):
- **수정(fix)**:
- **영향 파일(affected files)**:
- **검증(verification)**:
- **재발 방지 / regression 고려**:
- **상태**: 해결 / 재발 / 관찰 중
