# features/ — 기능별 심화 지식

기능별로 별도 페이지가 필요할 만큼 지식이 쌓이면 여기 `<feature>.md`로 추가한다.
(현재는 골격만. 예: `nearby-places.md`, `exhibition.md`, `reviews.md`.)

- 아키텍처 개괄은 `../architecture/` 참조.
- 코드 위치는 `lib/features/<name>/`, `lib/presentation/screens/<name>/`.
