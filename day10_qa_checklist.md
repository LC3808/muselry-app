# Day 10 QA 체크리스트

> **목적**: MVP 출시 전 전체 플로우 통합 테스트 및 빌드 검증  
> **기준**: v1.8 명세서 기준, flutter analyze No issues found 상태에서 진행  
> **작성일**: 2026-05-13

---

## 0. 사전 조건 확인

> **day8_fix_trigger.sql 정체 — 분류 B (안전장치, 필수 아님)**
> 
> `day8_reviews_v2_case_a.sql`은 `reviews` 테이블을 DROP 후 재생성하므로 `trg_update_museum_rating` 트리거가 함께 삭제됩니다. `day8_fix_trigger.sql`은 이 상황을 대비해 트리거를 재등록하는 안전장치로 작성되었습니다. 단, `day8_supplement.sql`이 이미 동일한 트리거를 `CREATE OR REPLACE` 방식으로 재등록하며, 검증 쿼리(⑦)에서 3행 정상 확인이 완료된 상태입니다. 따라서 **현재는 실행 불필요**합니다. 재실행해도 멱등성이 보장되므로 안전하지만, 이미 통과된 검증을 번복하지 않아도 됩니다.

- [x] `day8_reviews_v2_case_a.sql` 실행 완료 (reviews 테이블 v2 재생성, visit_id NOT NULL FK 포함)
- [x] `day9_museum_ranking_rpc.sql` 실행 완료 (refresh_museum_ranking RPC 함수 등록)
- [x] `day8_day9_verification_queries.sql` 검증 완료 (10개 항목 모두 통과)
- [ ] flutter analyze No issues found 확인
- [ ] Supabase 연결 정상 (개발/스테이징 환경)

---

## 1. 인증 플로우

| # | 테스트 항목 | 기대 결과 |
|---|-------------|-----------|
| 1-1 | 이메일/비밀번호 회원가입 | 프로필 자동 생성, 홈 화면 이동 |
| 1-2 | 로그인 | JWT 발급, 세션 유지 |
| 1-3 | 로그아웃 | 세션 삭제, 로그인 화면 이동 |
| 1-4 | 비로그인 상태에서 북마크/방문/리뷰 시도 | "로그인이 필요합니다" 안내 표시 |
| 1-5 | Kakao 소셜 로그인 | 카카오 OAuth 인증 → 프로필 자동 생성/연동, 홈 화면 이동 |
| 1-6 | Google 소셜 로그인 | 구글 OAuth 인증 → 프로필 자동 생성/연동, 홈 화면 이동 |
| 1-7 | Apple 소셜 로그인 (iOS 전용) | 애플 OAuth 인증 → 프로필 자동 생성/연동, 홈 화면 이동. iOS 빌드 인증서/프로비저닝 프로파일 확인 필수 |
| 1-8 | 소셜 로그인 후 재실행 | 세션 자동 복원, 재인증 불필요 |
| 1-9 | 소셜 로그인 → 로그아웃 → 재로그인 | 동일 계정 연속 로그인 정상 동작 |

---

## 2. 홈 화면

| # | 테스트 항목 | 기대 결과 |
|---|-------------|-----------|
| 2-1 | 인기 박물관 가로 스크롤 섹션 표시 | bayesian_score 상위 10개 카드 표시 |
| 2-2 | 어린이 친화 박물관 카드에 👶 배지 표시 | is_kids_friendly=true인 박물관에 배지 |
| 2-3 | 인기 박물관 카드 탭 → 상세 화면 이동 | 해당 박물관 상세 화면 정상 표시 |
| 2-4 | 당겨서 새로고침 | 인기 박물관 목록 갱신 |

---

## 3. 탐색 화면

> **v1.8 필터 구조**: 추천 태그 줄 / 유형 필터 줄 / 운영 필터 줄 3분류 독립 운영. "아이와 함께"는 유형 필터와 분리된 추천 태그 줄에 위치합니다.

| # | 테스트 항목 | 기대 결과 |
|---|-------------|-----------|
| 3-1 | 추천 태그 "아이와 함께" 선택 | is_kids_friendly=true 박물관만 표시 |
| 3-2 | 유형 필터 칩 선택 (박물관/미술관/과학관/기념관/전시관) | museums.type 기준 필터링 |
| 3-3 | 운영 필터 칩 선택 (공공) | 국립+공립 그룹 기준 필터링 |
| 3-4 | 운영 필터 칩 선택 (민간) | 사립+대학+기업 그룹 기준 필터링 |
| 3-5 | 추천 + 유형 복합 필터 | 두 조건 AND 적용 |
| 3-6 | 추천 + 운영 복합 필터 | 두 조건 AND 적용 |
| 3-7 | 유형 + 운영 복합 필터 | 두 조건 AND 적용 |
| 3-8 | 추천 + 유형 + 운영 세 조건 복합 | 세 조건 AND 적용 |
| 3-9 | 검색어 입력 | 이름/주소 기준 검색 결과 표시 |
| 3-10 | 필터 초기화 | 전체 목록 복원 |
| 3-11 | 무한 스크롤 | 20건씩 추가 로드 |

---

## 4. 박물관 상세 화면

| # | 테스트 항목 | 기대 결과 |
|---|-------------|-----------|
| 4-1 | 기본 정보 표시 (이름, 유형, 주소, 전화, 운영시간) | 정상 표시 |
| 4-2 | 어린이 친화 배지 표시 (is_kids_friendly=true인 경우) | 👶 어린이 친화 배지 표시 |
| 4-3 | kids_note 섹션 표시 (kidsNote가 있는 경우) | 어린이 안내 섹션 표시 |
| 4-4 | 북마크 버튼 토글 | 북마크 추가/제거 정상 동작 |
| 4-5 | "다녀왔어요" 버튼 → 방문 기록 추가 | visits 테이블에 레코드 생성 |
| 4-6 | 리뷰 목록 표시 (published 리뷰만) | 별점, 내용, 작성자 표시 |
| 4-7 | 지도 표시 (위경도 있는 경우) | 네이버 지도 마커 표시 |

---

## 5. 리뷰 작성 플로우 (1방문 1리뷰 핵심 검증)

| # | 테스트 항목 | 기대 결과 |
|---|-------------|-----------|
| 5-1 | 방문 기록 없는 상태에서 리뷰 쓰기 탭 | "방문 기록을 먼저 남겨주세요" + "다녀왔어요" 버튼 표시 |
| 5-2 | 방문 기록 1건 있는 상태에서 리뷰 쓰기 탭 | 해당 방문 선택 화면 표시 |
| 5-3 | 방문 기록 여러 건 있는 상태에서 리뷰 쓰기 탭 | 방문 목록 선택 화면 표시 |
| 5-4 | 리뷰 작성 완료 | visit_id 포함하여 DB 저장, 별점/내용 표시 |
| 5-5 | 이미 리뷰가 있는 방문 선택 | "리뷰 수정하기" 버튼 표시 |
| 5-6 | 리뷰 수정 (7일 이내) | 내용/별점 수정 반영 |
| 5-7 | 리뷰 삭제 | status=removed 처리, 목록에서 제거 |
| 5-8 | 리뷰 작성 후 museums.review_count/average_rating 갱신 | 트리거 동작 확인 |

---

## 6. 방문 기록 화면

| # | 테스트 항목 | 기대 결과 |
|---|-------------|-----------|
| 6-1 | 방문 기록 목록 표시 | 최신순 정렬, 박물관명/날짜 표시 |
| 6-2 | 리뷰 작성 완료 방문 카드 | "리뷰 작성 완료" 배지 + "수정하기" 버튼 |
| 6-3 | 리뷰 미작성 방문 카드 | "리뷰 쓰기" 버튼 표시 |
| 6-4 | 방문 기록 삭제 (리뷰 없는 경우) | 확인 다이얼로그 표시 → 삭제 완료, 리뷰 경고 없음 |
| 6-5 | 방문 기록 삭제 (리뷰 있는 경우) | 다이얼로그에 "이 방문에 작성된 리뷰도 함께 삭제됩니다" 경고 표시 |
| 6-6 | 방문 삭제 후 연결된 리뷰 DB 상태 | ON DELETE CASCADE → reviews 테이블에서 해당 visit_id 레코드 삭제 확인 |

---

## 7. 커뮤니티 화면

| # | 테스트 항목 | 기대 결과 |
|---|-------------|-----------|
| 7-1 | 최신 리뷰 피드 표시 | published 리뷰 20건, 최신순 |
| 7-2 | 작성자 아바타/닉네임 표시 | profiles 조인 결과 표시 |
| 7-3 | 박물관 헤더 탭 → 상세 화면 이동 | 해당 박물관 상세 화면 이동 |
| 7-4 | 무한 스크롤 | 추가 20건 로드 |
| 7-5 | 당겨서 새로고침 | 최신 리뷰 재로드 |

---

## 8. 마이페이지

| # | 테스트 항목 | 기대 결과 |
|---|-------------|-----------|
| 8-1 | 프로필 정보 표시 (닉네임, 아바타) | 정상 표시 |
| 8-2 | 방문 수 / 북마크 수 / 리뷰 수 통계 | 실제 DB 집계값과 일치 |
| 8-3 | "내가 다녀온 박물관 지도" 프리뷰 탭 | mypage_map_screen 이동 |
| 8-4 | 마이페이지 지도 화면 — 방문 마커 표시 | 진한 색 마커, 방문 횟수 표시 |
| 8-5 | 마이페이지 지도 화면 — 북마크 마커 표시 | 연한 색 마커 |
| 8-6 | 마이페이지 지도 화면 — 클러스터링 | 근접 마커 클러스터 표시 |
| 8-7 | 마이페이지 지도 화면 — 필터 (전체/방문/북마크) | 필터 적용 후 마커 갱신 |
| 8-8 | 마이페이지 지도 화면 — 통계 오버레이 | 총 방문 수, 이번 달 방문 표시 |
| 8-9 | 마이페이지 지도 화면 — 방문 마커 탭 | 방문 기록 Bottom Sheet 표시 |
| 8-10 | 방문+북마크 중복 박물관 마커 우선순위 | 방문 마커(진한 주황)가 북마크 마커(연한 파랑)보다 우선 표시 |
| 8-11 | 기간 필터 선택 — 전기간/이번 달/올해 | 필터 적용 후 해당 기간 방문 마커만 표시, 나머지는 북마크 마커로 표시 |
| 8-12 | 기간 필터 적용 시 중복 마커 동작 | 기간 내 방문 없고 북마크만 있는 경우 북마크 색상으로 표시 |

---

## 9. 지도 화면 (전체 박물관 지도)

| # | 테스트 항목 | 기대 결과 |
|---|-------------|-----------|
| 9-1 | 전체 박물관 마커 표시 | `SELECT COUNT(*) FROM museums` 결과와 동일한 마커 또는 클러스터 표시 |
| 9-2 | 마커 탭 → 박물관 정보 표시 | 박물관명, 유형, 북마크 버튼 |
| 9-3 | 유형별 필터 | 해당 유형 마커만 표시 |

---

## 10. 빌드 검증

| # | 테스트 항목 | 기대 결과 |
|---|-------------|-----------|
| 10-1 | flutter analyze | No issues found |
| 10-2 | Android 테스트 빌드 (`flutter build apk --debug`) | 빌드 성공, 설치 가능 |
| 10-3 | iOS 테스트 빌드 (`flutter build ios --debug --no-codesign`) | 빌드 성공 |

---

## 11. 인기 박물관 ↔ 리뷰 연쇄 동작 (통합)

| # | 테스트 항목 | 기대 결과 |
|---|-------------|-----------|
| 11-1 | 리뷰 작성 → museums.review_count 증가 | 트리거 즉시 반영 |
| 11-2 | 리뷰 작성 → museums.average_rating 갱신 | 트리거 즉시 반영 |
| 11-3 | museum_ranking RPC 재실행 → bayesian_score 갱신 | `SELECT refresh_museum_ranking();` |
| 11-4 | 홈 화면 인기 박물관 카드 순위 반영 | 당겨서 새로고침 후 순위 변경 확인 |

---

## 12. 엣지 케이스

| # | 테스트 항목 | 기대 결과 |
|---|-------------|-----------|
| 12-1 | 네트워크 오프라인 상태 | 에러 스낵바 표시, 앱 크래시 없음 |
| 12-2 | 리뷰 내용 500자 초과 입력 | 입력 제한 또는 에러 표시 |
| 12-3 | 리뷰 내용 10자 미만 입력 | 제출 버튼 비활성화 또는 에러 |
| 12-4 | 동일 방문에 리뷰 중복 작성 시도 | DB unique 제약 에러 처리 |
| 12-5 | 방문 기록 삭제 후 연결된 리뷰 상태 | ON DELETE CASCADE로 리뷰도 삭제 |

---

## 13. DB 검증 쿼리 (Supabase SQL Editor에서 직접 실행)

아래 쿼리는 Supabase 대시보드 → SQL Editor에서 실행하여 결과를 확인합니다.

### ⑥ review_reports UNIQUE 제약 확인

```sql
SELECT
  tc.constraint_name,
  kcu.column_name
FROM information_schema.table_constraints tc
JOIN information_schema.key_column_usage kcu
  ON tc.constraint_name = kcu.constraint_name
  AND tc.table_schema = kcu.table_schema
WHERE tc.table_name = 'review_reports'
  AND tc.constraint_type = 'UNIQUE'
ORDER BY kcu.ordinal_position;
```

**기대 결과**: `review_id`, `reporter_id` 두 컬럼이 UNIQUE 제약에 포함되어 있어야 합니다.

### ⑨ 인기 박물관 상위 10개 조회

```sql
SELECT
  mr.rank_overall,
  m.name,
  mr.bayesian_score,
  m.review_count,
  m.average_rating
FROM museum_ranking mr
JOIN museums m ON mr.museum_id = m.id
ORDER BY mr.rank_overall
LIMIT 10;
```

**기대 결과**: 상위 10개 박물관이 `bayesian_score` 내림차순으로 정렬되어 표시되어야 합니다.

---

## 완료 기준

**필수** — 아래 항목 모두 통과 시 Day 10 완료 처리 및 MVP 출시 준비 완료로 판단합니다.

- 0번 사전 조건 전체 확인
- 1~11번 전체 통과
- 13번 DB 핵심 검증 통과

특히 아래 항목은 MVP 핵심 검증으로 반드시 통과해야 합니다:

- 리뷰 작성 후 `review_count` 증가 (트리거 동작 확인)
- 리뷰 작성 후 `average_rating` 갱신 (트리거 동작 확인)
- `refresh_museum_ranking()` 실행 후 `bayesian_score` 갱신
- 홈 인기 박물관 카드 순위 반영
- `reviews.visit_id NOT NULL` 제약 확인
- `reviews_unique_active_visit` 부분 인덱스 확인
- `review_reports UNIQUE(review_id, reporter_id)` 제약 확인
- `museum_ranking` row 수 확인

**권장** — 시간 여유에 따라 진행합니다.

- 12번 엣지 케이스

---

## 이슈 분류 기준

QA 중 이슈 발생 시 아래 기준으로 분류하여 체크리스트 항목 번호와 함께 보고합니다.

| 등급 | 기준 | 처리 방침 |
|------|------|-----------|
| **P0** | 앱 실행 불가, 로그인 불가, DB 마이그레이션 실패, 리뷰/방문 기록 핵심 플로우 작동 불가, 빌드 실패 | 즉시 중단 후 픽스, 재테스트 |
| **P1** | 리뷰 작성/수정/삭제/신고 일부 실패, 1방문 1리뷰 정책 위반, 마이페이지 지도 주요 기능 실패, 인기 박물관 데이터 갱신 실패, 필터 조건 오적용 | MVP 포함 여부 판단 후 픽스 |
| **P2** | 일부 UI 깨짐, 특정 필터 조합 오류, 빈 상태/에러 메시지 미흡, 성능 저하 | Phase 1.5 이관 검토 |
| **P3** | 문구 수정, 색상/아이콘 조정, 향후 개선 항목 | 백로그 등록 후 계속 진행 |
